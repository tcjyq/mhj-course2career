# 安全策略

## 支持范围

当前维护版本为 `0.1.x`。本项目仍处于作品集和公开演示准备阶段，不应在未完成公网安全加固前处理敏感或机密数据。

## 报告安全问题

请通过仓库维护者在 GitHub 个人资料中提供的私密联系方式报告安全问题，不要创建公开 Issue。

报告中请包含：

- 受影响的模块和版本；
- 可复现步骤；
- 潜在影响；
- 建议的缓解方式（如有）。

请勿在报告中附带真实 API Key、密码、Token、个人课程信息或招聘方机密 JD。

## 凭证处理

- 平台密钥只通过环境变量提供；
- 初始管理员凭证只通过部署平台 Secrets 提供，生产环境优先使用预生成的 scrypt 哈希，不设置通用默认账号；
- 管理员密码轮换会递增会话版本，使旧密码和旧登录会话失效；
- 登录连续失败会按浏览器会话和用户名进入短时冷却，但公网部署仍需补充可信代理层的 IP 限速；
- `.env`、Streamlit secrets、本地数据库和私钥文件不得提交；
- 开发者 API Key 使用 AES-256-GCM 加密后存储；
- 开发者 API Key 提交后必须立即清空输入框与对应会话状态，后续页面仅显示末四位；
- DeepSeek 模型目录缓存只使用 API Key 的 SHA-256 指纹作为隔离标识，不保存或输出 Key；
- GitHub 模型监测使用仓库 Actions Secret，未知模型只创建评审 Issue，不自动进入线上白名单；
- 如果凭证曾进入 Git 历史，应立即撤销并重新签发，仅删除文件不足以消除风险。
