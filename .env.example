# 平台模型凭证：本地规则模式可全部留空。
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
OPENAI_TIMEOUT_SECONDS=30

DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash

# 本地开发默认使用 SQLite；运行时目录不会提交到 Git。
COURSE2CAREER_DATABASE_PATH=instance/course2career.db

# URL-safe Base64 编码的 32 字节随机值，仅在开发者 API Key 功能启用时需要。
COURSE2CAREER_KEY_ENCRYPTION_KEY=

# 可选：首次启动时创建管理员。密码至少 12 位，只能放在本地环境或部署平台 Secrets。
COURSE2CAREER_BOOTSTRAP_ADMIN_USERNAME=
COURSE2CAREER_BOOTSTRAP_ADMIN_PASSWORD=
