# Course2Career 开发日志

## 2026-09-24 C08-D1 持久化证明与大陆供应商验证

在 D0 的 PostgreSQL 合成集成测试后增加 CI 容器内 `pg_dump`→空库 `pg_restore`，核对持久表、schema 版本和恢复后密文解密；增加独立远程测试库脚本与[备份恢复操作说明](c08-backup-restore-runbook.md)。本地补测生产库不可达／TLS 不合格时无 SQLite 回退、错误主密钥安全失败，以及 Streamlit 根级 Secrets 与环境变量配置路径一致。CI、远程库和恢复演练的实际结果以运行记录为准，未运行的环节保持 PENDING。

本机未提供 `C2C_TEST_DATABASE_URL`，DeepSeek、百炼、SiliconFlow 凭证也不可用；未运行独立远程 PostgreSQL 或真实大陆模型调用。`PERSISTENCE_READY=no`、`PROVIDER_VALIDATED=no`、`RELEASE_READY=no`。D1 feature 分支与 Draft PR 只用于 CI，不能合入或部署。

## 2026-09-24 C08-C 大陆优先官方模型目录（本地，未发布）

新增十家受控预设的目录策略：DeepSeek、百炼、SiliconFlow、MiniMax、OpenRouter、OpenAI、Anthropic、Gemini 动态读取官方模型 API，Kimi/GLM 暂用注明来源日期的短目录。官方可用性/能力/价格与 B2 真实验证分层；DeepSeek 当前 `deepseek-flash` 为默认候选，旧 `deepseek-v4-flash` 只作为兼容别名提示用户主动迁移。百炼按区域/Workspace 查询，MiniMax 区分国内与国际端点。缓存按用户、端点、Workspace 和 Key 版本隔离，支持 TTL、手动刷新与失败旧目录；B3 关闭即拒绝读取。Provider Hub 大陆优先，国际折叠；分析页按模型级证据排序并提示未知/未验证。文档与来源见 [模型目录](c08-model-discovery.md)、[官方矩阵](c08-provider-matrix.md)、[ADR 006](decisions/006-model-catalog-provenance-and-scope.md)。

本地 Python 3.12/3.14 各 224 项离线测试通过；Ruff、格式、3.14 `pip check` 通过。隔离 Streamlit 浏览器用假账号/Key 验证 Free 用户开关、Kimi 目录选模与刷新、配置恢复、国际折叠、分析页全部已发现模型和未验证警告；390px `scrollWidth=390`，控制台 0 错误/警告。没有合法大陆 Key，本轮真实 discovery 和 B2 模型调用均为 0；所有真实兼容性仍待验证。

## 2026-09-23 C08-B3 开发者模式自助启用（本地，未发布）

普通注册用户可免费开启或关闭独立的 BYOK capability，不改变 Role、Plan 和平台系统 AI 额度。新 `user_byok_settings` 表持久化用户 ID、开关和更新时间；Admin 与历史 Developer Role/Plan 兼容。Key、Profile 与用户 Key 调用在服务层读取最新状态，关闭后拒绝新操作但保留 AES-256-GCM 密文。会员页把 Free/Pro 套餐演示与免费开发者模式分开，开发者页提供介绍、启用、Provider Hub 和关闭入口。决策见 [ADR 005](decisions/005-self-service-byok-capability.md)。

权限与迁移回归覆盖 Free/Pro 额度、旧会话、跨用户隔离、历史用户、重复初始化与回滚。本轮浏览器使用隔离账号和假 Key，完成注册、登录、启用、保存、刷新后重新登录、关闭与重开；390px 页面宽度无整体溢出，见[截图](../screenshots/c08-b3-developer-mobile.png)。直接访问子路由与刷新时仍会出现 Streamlit `_stcore` 两条相对路径 404，页面可继续使用。本轮不进行真实供应商调用；最终检查结果见本次交付报告。未推送或部署。

## 2026-09-23 C08-B2 真实 Provider 兼容性验证（本地，未发布）

新增协议输出策略、Anthropic/Gemini/百炼小型 schema adapter、精确 Provider × 官方端点 × 模型验证记录、4 个固定合成 JD 和脱敏错误分类。连接测试最多达到 `SCHEMA_COMPATIBLE`，完整 fixture、usage、返回模型和原文 evidence 均通过才可标 `VERIFIED`；DeepSeek 历史 Auto-Safe 白名单不再直接显示为 B2 VERIFIED。百炼默认候选改为 `qwen3.8-flash`，OpenRouter 需模型 metadata 证明支持 strict schema。决策见 [ADR 004](decisions/004-provider-validation-evidence.md)，官方依据与真实记录见 [B2 验证报告](c08-provider-validation.md)。

本次只检测环境变量存在性，不读取或输出凭证内容。OpenAI `gpt-5.6-luna` 与 Anthropic `claude-haiku-4-5-20251001` 各真实请求 1 次，均 HTTP 401／`AUTH_ERROR`；因此未执行后续 fixture，0 家 VERIFIED、0 家 CONNECTED、8 家无 Key PENDING。无 usage 回执，费用不可估算。原始错误、请求头、Key 和真实 JD 未写入文档或仓库；本地 JSON 与摘要位于 gitignored `outputs/c08-provider-validation/`。未推送、未部署、未改生产 Secrets。

本地 Python 3.12 与 3.14 各 207 项 pytest 通过；Ruff lint、format、3.14 `pip check` 与 `git diff --check` 通过。此自动化结果只验证代码与 fake 编排，真实兼容性仍受可用凭证限制。C08-C 继续模型发现、能力、价格和上下文元数据。

## 2026-09-23 C08-B1 主流 Provider BYOK 基础（本地，未发布）

新增十家官方受控预设和四类协议路由，保留旧 OpenAI Responses 与 DeepSeek Auto-Safe；Developer/Admin 统一卡片可加密保存、更新、删除 Key，配置端点 ID／模型 ID，分析页只列当前账户已配置 Provider。新增 Anthropic Messages、Gemini Native 适配器、合成 JD 连接测试契约和模型级验证状态。SQLite 将旧 2/4 值 Key 约束事务化扩到十值，保留原密文与 nonce，新增用户 Profile 和费用 `estimated/unknown`。官方来源见 [Provider 矩阵](c08-provider-matrix.md)，架构决定见 [ADR 003](decisions/003-protocol-based-byok-provider-presets.md)。本轮只使用 fake Key/响应，未做真实供应商调用、推送或部署；验证结果见本次交付报告。

安全复核时发现本地 OpenAI SDK 默认 HTTP 客户端会跟随重定向；本阶段为 OpenAI Responses、DeepSeek 调用／目录和共享 Chat 显式禁用重定向，原生 Anthropic/Gemini 同样拒绝重定向。本地隔离浏览器已走通游客/Free 权限、Developer 添加／更新／删除／重新登录后持久化、Admin 十卡与 390px 无整体溢出；强制浏览器刷新会丢失 Streamlit 会话并要求重新登录。直接刷新子路由时浏览器记录两条 Streamlit `_stcore` 相对路径 404，页面可继续显示；未将其归为供应商调用结果。

## 2026-09-23 C08-A 多供应商基础（本地，未发布）

从 `3c9378683640bfbeb1ddcaec1a909a8ca0286c82` 创建 `feature/c08-multi-provider`。新增四项受控 Provider 预设及百炼/OpenRouter 共享兼容 Chat 适配器；保留 DeepSeek Auto-Safe 和 OpenAI Responses 路径。免费套餐系统AI固定 DeepSeek；用户 Key 与系统额度分离。旧 SQLite API Key 表约束事务化扩展，密文原样保留。百炼/OpenRouter 尚未开放页面或真实调用；设计、验证和后续阶段见 [C08 设计](c08-multi-provider-design.md)。

2026-09-23：完成 C08-OSS 开源架构与许可研究，确认 C08-B 为十家主流受控预设 + Developer BYOK，C08-C 为模型发现，C08-D 为自定义 Provider。研究报告见 [C08-OSS](c08-open-source-research.md)。本轮仅更新文档，没有移植第三方代码、增加依赖或进行真实 API 调用。

本地验证：Python 3.12 与 3.14 全量 pytest 通过，Ruff 检查与格式检查通过，3.14 `pip check` 通过；隔离浏览器以假平台 Key 验证游客系统AI仅显示 DeepSeek，控制台无错误或警告，未发出模型请求。以上不代表真实供应商兼容性验证或生产发布。

## 记录规则

每次完成一个可验证的开发阶段后追加记录，包含日期、目标、完成内容、验证结果、技术决策和后续任务。时间统一使用东八区。

建议格式：

```text
## YYYY-MM-DD 阶段名称

### 本阶段目标

### 完成内容

### 验证结果

### 技术决策

### 已知问题

### 下一步
```

## 2026-09-21 求职导向证据与演示优化

2026-09-22 RC2运行时审计：用户确认生产Python为3.14，在隔离CPython 3.14.2环境安装全部原有pin并通过150项测试（22.82秒）；既有3.12.14环境150项通过（27.46秒）。CI增加3.14且保留3.11／3.12，部署文档明确Demo SQLite及C08正式上线前持久化技术债。业务源码、依赖pin和界面不变，真实3.14浏览器三例／上传／导出／390px复测完成。远端main新增Dev Container提交，已只读核对并刷新缓存，未合并或推送。完整边界见[RC2记录](optimization-review.md#2026-09-22-final-pre-publish-audit--rc2)。

后续发布候选复核：全量150项通过（24.63秒），Ruff、格式、差异和本地浏览器复测通过；仅纠正文档4处行为描述，保留C01—C07核心实现。降级读取新快照的限制及数据保护要求见[Release Candidate Verification](optimization-review.md#release-candidate-verification)。按用户授权形成独立本地提交，推送与部署仍等待授权；不进入C08／C09。

### 完成内容

按优化规范复核HEAD后完成C01—C07：修正通用课程到具体工具的误推断，完整显示直接／间接／迁移／自述来源，校正资料完整度与空门槛文案，增加JD引用严格核对和人工修正后重查，加入三方向合成案例及可下载输入包，学习任务提供交付物和验收标准。README补90秒入口与真实clone命令，Showcase仅同步文字。详细对照、文件列表、兼容边界和证据见[本次交付记录](optimization-review.md)。

### 验证结果

Windows Python3.12.14，全量150 passed、0 failed、0 skipped（25.56秒）；Ruff lint、format和git diff --check通过。首次测试受系统临时目录权限影响，改用D盘隔离目录验证，未删测试或降低断言。最后格式化及迁移说明措辞调整另作相关回归与格式检查。

真实浏览器检查桌面和窄屏三例、来源展开、独立重置、旧版与v2.1历史恢复及下载；AI例Markdown、数据例CSV下载与后端逐字节一致。浏览器启动方式的差异如交付记录：首页导航及历史夹具无控制台错误；直接/analysis启动出现两次探测404后恢复。截图存于screenshots/optimization-20260921，非人工用户验收。

### 决策与范围

保持原框架、provider接口、五维权重、基础分和旧报告。sources与任务新字段可选，不迁移生产数据，不改变账号与计费。CSV原五列保持兼容，完整来源在界面和Markdown。只有错误课程规则修正影响相关分数；相同课程规则下三例与HEAD旧引擎无五维分或总分漂移。没有真实模型调用。

### 未完成的外部验证

业务效果、人工标注准确率、真实用户任务效果均未校准；Python3.11与远端CI本次未运行。P2仅给方法。本人历史贡献待确认，未修改作品集、简历PDF或其他仓库。未commit、push或部署，不能将本地完成描述为已经上线。

## 2026-09-20 招聘 Showcase 与无 Key 游客路径

### 本阶段目标

为招聘者提供稳定的项目说明入口，同时不改变 Streamlit 的规则模型或评分逻辑。

### 完成内容

- 新增 `showcase/` Cloudflare 静态资产 Worker，目标域名为 `course2career.tcjyq.cc`。
- 新增 Hero、问题、七步工作流、证据、真实首页截图、个人职责和外部链接；截图逐字节复用 `screenshots/home.png`。
- README 公开展示明确列出 Showcase 与 Live Demo 的完整 URL。
- 未配置平台模型凭证时隐藏“系统AI”，游客默认使用本地规则完成核心分析；未增加公开付费 API Key。

### 技术决策

- Showcase 仅返回静态资产与安全响应头，不依赖 Python、Streamlit、数据库或 Secrets，也不将自定义域名重定向到 Streamlit。
- Community Cloud 休眠是平台行为；不使用定时 ping、保活脚本或其他规避方式。
- 本次未修改技能映射、硬门槛或 Career Adaptability Model v2.1 评分逻辑。

### 验证结果

- 自动化、Ruff、格式、静态 Worker 检查和匿名浏览器验收以本次发布记录为准。

### 下一步

- Showcase 验收通过后，再统一替换五份简历中的 Course2Career 链接。

## 2026-07-22 项目初始化

### 本阶段目标

根据已确认的 MVP 方案建立项目基础结构，不实现业务逻辑。

### 完成内容

- 创建 Streamlit 应用入口占位文件。
- 创建核心 Python 包及模块占位文件。
- 创建数据、Prompt、示例、测试和文档目录。
- 添加 README、Python 依赖清单和 Git 忽略规则。
- 添加环境变量示例文件，但未写入任何凭证或供应商配置。

### 验证结果

- 项目目录和文件已创建。
- 业务模块和测试文件均为空。
- 当前尚无可运行的应用功能。

### 技术决策

- MVP 使用单体 Streamlit 架构，不拆分 FastAPI 后端。
- 大模型供应商尚未确定，因此依赖清单暂不加入供应商 SDK。
- 依赖暂不锁定具体版本，首次实现前统一核对兼容性并锁定。
- 不在 MVP 中引入数据库、FAISS、向量数据库或多智能体框架。

### 已知问题

- 当前目录尚未初始化为 Git 仓库。
- 需求、架构、评分和评测文档仍是空占位文件。
- Excel 示例模板将在课程导入阶段生成，避免提交无效的空 `.xlsx` 文件。

### 下一步

- 初始化 Git 仓库并建立首个提交。
- 完成 `docs/requirements.md` 和 MVP 验收标准。
- 确认大模型供应商及配置方式。
- 开始课程 Excel 导入与校验模块。

## 2026-07-22 课程 Excel 导入与校验

### 本阶段目标

完成第一条可运行、可测试的功能链路：上传课程表、校验数据并预览合法课程和错误行。

### 完成内容

- 使用 Pydantic 定义课程、行级错误和导入结果模型。
- 实现 `.xlsx` 首个工作表读取和必填列校验。
- 实现空白行忽略、字段范围校验和重复课程识别。
- 实现 Streamlit 文件上传、课程预览和错误展示。
- 增加项目打包配置和可编辑安装方式。
- 补充课程导入需求、README 和开发命令。

### 验证结果

- 课程解析器包含 7 个自动化测试。
- Streamlit 页面包含 3 个自动化流程测试。
- 有效文件、无效文件、缺列、非法字段、空行和重复课程均有覆盖。

### 技术决策

- 文件级错误使用异常中止导入，行级错误使用结果列表返回。
- 合法行和非法行可以同时存在，避免一行错误阻断整个文件。
- 仅支持 `.xlsx` 并显式使用 `openpyxl`，暂不扩大格式范围。
- 项目使用 `src` 布局，通过 `pyproject.toml` 进行可编辑安装。

### 已知问题

- 尚未提供课程 Excel 模板下载。
- 尚未接入 JD、技能映射和评分模块。
- 当前只读取第一个工作表。

### 下一步

- 创建可下载的课程 Excel 模板。
- 定义 JD 技能结构化数据模型和测试样例。
- 确认大模型供应商后实现 JD 技能提取接口。

## 2026-07-22 MVP 全功能实现

### 本阶段目标

完成从课程与 JD 输入到匹配报告输出的全部 MVP 功能，并保留无 API Key 的完整演示路径。

### 完成内容

- 增加 Excel 模板下载、技能别名词典、课程映射规则和学习资源。
- 实现本地规则与 OpenAI Responses API 两种 JD 提取模式。
- 实现人工技能确认、课程技能证据、确定性评分和学习路线。
- 实现 Markdown 报告与 UTF-8 CSV 明细导出。
- 完成 Streamlit 端到端交互和示例 JD。
- 补充需求、架构、评分、评测和 README 文档。

### 技术决策

- OpenAI 为可选增强；默认本地规则模式保证项目可离线演示。
- 大模型只提取结构化语义，所有匹配分均由 Python 公式计算。
- 学习路线使用版本化本地资源，避免生成不可验证的链接。
- 同一技能采用最强课程证据作为支撑分，同时保留全部证据供用户查看。

### 验证结果

- 29 项自动化测试全部通过。
- Ruff 静态检查和格式检查全部通过。
- Streamlit 本地健康检查返回 HTTP 200。

### 已知问题

- 课程映射词典覆盖的是信息管理常见课程，需要根据真实课程继续扩充。
- AI 黄金样例尚需人工标注后才能完成效果指标评测。
- 当前不保存历史报告，刷新会话后需要重新分析。

### 下一步

- 使用真实但脱敏的课程与 JD 样例扩充规则并开展黄金集评测。
- 增加 GitHub Actions，自动运行 Ruff 和 pytest。
- 为 GitHub 作品集补充当前版本截图和演示说明。

## 2026-07-23 用户权限系统

### 本阶段目标

在不修改原有分析服务的前提下，增加角色权限、注册登录、AI日额度、历史记录和管理员状态。

### 完成内容

- 增加guest、user、developer、admin四类角色及显式权限表。
- 增加SQLite用户、AI调用和分析历史仓储。
- 使用scrypt保存密码哈希，公开注册只能创建普通用户。
- 游客平台AI每天2次，普通用户和开发者每天5次，管理员不限额。
- 开发者可使用仅存在于当前会话的自带API Key。
- 登录用户的分析报告按用户ID保存和隔离。
- 管理员可以查看聚合系统状态。
- 增加权限、认证、额度、历史和页面测试。

### 验证结果

- 47项自动化测试全部通过。
- Ruff静态检查与格式检查全部通过。

### 技术决策

- 权限层位于模型调用和分析记录外层，原有AnalysisService保持不变。
- 当前使用SQLite，仓储边界为后续PostgreSQL迁移保留空间。
- 自带API Key当前不持久化，避免提前引入密钥加密基础设施。

### 已知问题

- 游客额度当前按匿名Streamlit会话计算，生产公开部署前需要网关级限流。
- developer和admin角色不允许公开注册，需要受信任的管理流程授予。
- AI调用记录已支持次数统计，Token与成本将在模型客户端返回用量元数据后接入。

## 2026-07-23 开发者API Key与多供应商模式

### 本阶段目标

在不修改JD分析与评分业务逻辑的前提下，支持系统API和开发者自带API Key，并兼容OpenAI与DeepSeek。

### 完成内容

- 增加供应商无关的`LLMProvider`接口和供应商工厂。
- 保留OpenAI Responses API适配器，增加DeepSeek Chat Completions JSON适配器。
- 增加开发者Key保存、查询元数据、更新和删除能力。
- 使用AES-256-GCM、随机nonce及用户/供应商绑定数据加密Key。
- 平台Key与加密主密钥均通过环境变量注入。
- Streamlit增加供应商选择和开发者Key管理入口。
- AI调用日志记录实际供应商与模型。

### 技术决策

- 领域业务只依赖`LLMProvider`，新增供应商不需要修改分析服务。
- 数据库不保存明文Key，仅保存密文、nonce和末四位展示信息。
- 加密主密钥不进入数据库或代码仓库，避免数据库泄露后可直接解密。
- 本地规则模式保持独立，不要求任何API Key。

## 2026-07-25 管理员密码轮换与会话失效

- 管理员初始化配置发生变化时，安全更新现有管理员密码哈希，不创建第二个管理员。
- 用户记录增加会话版本；密码或会员权限变化后，旧会话在下一次页面交互时失效。
- 登录页面按“浏览器会话 + 规范化用户名”限制连续失败次数，5次失败后冷却15分钟。
- 保持错误信息通用，不区分用户名不存在、密码错误或账户不可用。
- 接通OpenAI Responses与DeepSeek Chat Completions返回的真实Token用量。
- 为所有模型设置1500输出Token上限，并支持按部署配置估算调用费用。

## 2026-07-25 会员权限模型

### 完成内容

- 增加Free、Pro、Developer、Admin套餐枚举。
- 将角色安全边界与套餐产品能力拆分，权限判断要求两层同时通过。
- Free每天5次平台AI，Pro和Developer每天20次，Developer自带Key不限平台额度，Admin不限额。
- 增加高级报告权限位，暂不开发高级报告业务内容。
- 增加管理员套餐分配服务，作为未来支付完成后的会员生效边界。
- 增加旧developer/admin用户套餐数据兼容迁移。

### 当前边界

- 未接入微信支付、支付宝、Stripe或其他支付SDK。
- 未实现订单、订阅、价格、回调、退款和会员有效期。
- 未开放用户自助升级入口，套餐变更仅允许管理员执行。

## 2026-07-25 管理员Dashboard

### 完成内容

- 在Streamlit中增加管理员Dashboard与分析工具视图切换。
- 展示用户数量、今日分析次数、AI调用次数、Token消耗和预计费用。
- 增加不包含密码与API Key字段的用户列表。
- 管理员可以通过会员服务修改用户套餐和对应角色。
- 防止管理员在当前会话中取消自己的Admin权限。

### 技术决策

- 继续使用单体Streamlit，不拆分独立后台项目。
- 今日统计按东八区自然日计算，数据库时间仍使用UTC。
- Token与预计费用聚合已有AI调用日志，不引入新的计费引擎。
- 当前不实现分页、复杂图表、支付管理和操作审计。

## 2026-07-25 Streamlit产品界面重构

### 完成内容

- 将原有单页工具重构为七类产品页面和角色动态导航。
- 增加首页、独立登录页、AI额度页和会员升级演示页。
- 将完整课程分析流程迁移到个人分析页面。
- 将开发者API Key从侧边栏表单迁移到独立页面，并支持删除。
- 保留管理员Dashboard，只有管理员身份显示管理导航。
- 增加暖灰、深绿、细边框的简约主题和页面空状态。

### 技术决策

- 使用Streamlit原生`st.navigation`和页面函数，不增加独立前端工程。
- 页面层只负责状态与展示，分析、权限、额度和Key服务保持独立。
- 会员升级页为Demo，不调用套餐变更服务，不创建订单或支付。
- 视觉上不使用渐变、重阴影、夸张圆角和装饰性图标。

### 验证

- 68项自动化测试全部通过。
- Streamlit服务健康检查返回`ok`。

## 2026-08-03 报告历史恢复

### 背景

Streamlit 的当前页面内容位于浏览器会话状态中，刷新、重新登录或应用重启后会消失。登录用户的完整报告虽已保存，但此前页面只能显示摘要。

### 完成内容

- 增加按当前用户 ID 与报告 ID 读取完整快照的仓储和服务接口；
- 在“最近分析”中增加选择和重新打开报告操作；
- 兼容早期技能匹配报告与 Career Adaptability Model v2.1；
- 明确不持久化或回填课程 Excel、完整 JD 与敏感凭证。

### 回归修复

- 修复旧版 `AnalysisReport` 被当作 v2.1 报告渲染时访问不存在字段而崩溃的问题；
- 旧版报告改为独立兼容视图，并保留旧版 Markdown 和 CSV 导出；
- 历史选择器改用唯一报告 ID，避免相同时间、岗位和分数的记录被字典键覆盖；
- 登录失效或退出登录时同时清理旧版报告会话状态；
- 增加 Streamlit AppTest，覆盖 v2.1 恢复、旧版恢复和重复摘要三类场景。

### 验证结果

- 102 项自动化测试全部通过；
- Ruff 静态检查和格式检查全部通过；
- Git 差异空白检查通过；
- Streamlit 真实服务健康检查返回 `200 ok`。

### 后续边界

该修复解决刷新后的报告恢复。线上长期保存仍需将 Streamlit Community Cloud 的本地 SQLite 迁移至外部 PostgreSQL。

## 2026-07-26 Career Adaptability Model v2.1

### 本阶段目标

将过度依赖技能完全匹配的“综合匹配分”升级为适合大学生实习和校招场景的岗位适配度，同时保留确定性规则和可解释性。

### 完成内容

- 增加技术、教育、项目、实习和学习潜力五维适配度。
- 将学历、专业、毕业年份、到岗时间和城市作为独立硬门槛。
- 缺少技能直接证据时保留15分学习基础，不直接记零。
- 增加课程、项目、实习证据与技能迁移规则。
- 区分“暂未填写”和“目前没有”，未填写使用中性值并降低可信度。
- 增加从50分基线展开的逐项加减分账本。
- 学习路线改为最多8个能力模块。
- 更新Markdown报告、历史保存和Streamlit个人分析流程。
- 修正Python课程错误弱关联Git的问题，并让Python与Web后端课程能够迁移支持FastAPI。

### 验证结果

- 99项自动化测试全部通过。
- Ruff静态检查和格式检查全部通过。
- Streamlit真实服务健康检查返回`200 ok`。
- 代表性AI大模型应用开发实习案例得到67.3分，硬门槛满足，解释账本与最终分数一致。

### 技术决策

- 模型权重为技术25%、教育20%、项目20%、实习25%、潜力10%。
- 大模型继续只提取JD语义，不直接决定分数。
- 硬门槛不作为总分乘数，避免把资格限制误解为能力不足。
- 院校和学历作为现实市场信号参与解释，但不得表述为个人能力上限。
- 详细取舍记录在`docs/decisions/001-career-adaptability-model-v2-1.md`。

### 文档同步

- 同步更新README、需求、架构、评分、使用说明、界面设计和评测方案。
- 在根目录增加`AGENTS.md`，要求后续功能变更同时更新代码、测试、文档、示例和必要截图。

### 已知问题

- 当前首页截图来自旧版定位文案，需要在新版部署稳定后重新生成。
- 权重和院校市场信号属于可解释产品基线，尚未通过真实招聘结果校准。

## 2026-08-06 DeepSeek Auto-Safe模型管理

### 本阶段目标

让公开Demo能够适应DeepSeek模型目录变化，同时避免未经验证的新模型被自动投入线上。

### 完成内容

- 增加官方模型目录读取、外部字段校验、内存缓存和旧缓存回退；
- 增加Auto-Safe白名单交集选择和固定模型模式；
- 模型不存在时只允许一次已验证备用模型重试；
- 调用日志回写供应商实际返回的模型ID、Token和费用；
- 管理员Dashboard增加模型策略、缓存状态和手动刷新；
- 增加平台AI紧急开关与DeepSeek最大输出Token配置；
- 增加GitHub每日模型监测，未知模型只创建待验证Issue；
- 新增ADR 002并同步部署、架构、需求、使用和评测文档。

### 技术决策

- 自动发现不等于自动信任：未知模型必须经过契约和黄金集评测后人工加入白名单；
- 目录故障优先使用有效旧缓存，最终使用固定回退模型，保证功能可降级；
- 不引入通用模型路由框架，保持现有供应商边界和项目规模。

### 验证结果

- 117项自动化测试全部通过；
- Ruff静态检查和格式检查全部通过；
- Git差异空白检查通过。
- Streamlit真实服务健康检查返回`200 ok`。

## 2026-08-07 真实浏览器回归与导航修复

### 发现与修复

- Playwright 复现“首页切换到个人分析后，首页流程区仍残留”的内容串页问题；
- 页面内容改为可清空占位容器，路由变化时先清理旧内容并执行一次受控重跑；
- 真实浏览器复测确认个人分析页中的“使用流程”旧页面标题计数由1降为0；
- 浏览器控制台未发现应用错误或警告。

### 验证结果

- 117项自动化测试全部通过；
- Streamlit真实服务健康检查返回`200 ok`；
- Excel上传、完整本地规则分析、解释账本和报告生成继续由Streamlit AppTest覆盖。

## 2026-08-07 开发者 API Key 输入状态清理

### 问题与修复

- 真实浏览器测试发现，开发者 Key 加密保存成功后，密码输入框仍保留完整明文，浏览器可访问性快照可能读取该值。
- 根因是保存后立即执行 `st.rerun()`，表单自动清空尚未稳定生效。
- 改为 Streamlit 表单提交回调：回调内完成加密保存，并在成功或校验失败后清空密钥输入状态。
- 真实浏览器复测发现固定组件标识仍会让当前 DOM 暂时保留输入值；进一步改为提交后递增表单版本并创建全新密码组件。
- 删除真实浏览器测试产生的临时快照目录；测试和文档不包含真实凭证。

### 回归验证

- 增加 Streamlit AppTest，验证保存后 API Key 输入框为空且组件标识已经更新。
- 118 项自动化测试全部通过；
- Ruff 静态检查、格式检查和 Git 差异空白检查全部通过。

## 2026-08-07 Streamlit 热重载启动修复

### 问题与修复

- 新版部署后，Streamlit 进程短暂缓存旧版 `LLMProviderFactory`，而新版入口已传入 `model_catalog` 关键字，导致应用启动失败。
- 入口改为先使用稳定的双参数构造方式创建工厂，再注入共享模型目录；完整重启后行为与原设计一致。
- 增加热重载窗口回归测试，模拟旧工厂模块与新入口文件同时存在，确保页面仍可启动。

### 回归验证

- 119 项自动化测试全部通过；
- Ruff 静态检查、格式检查和 Git 差异空白检查全部通过。

## 2026-08-07 DeepSeek 真实调用用量回写修复

### 问题与修复

- 隔离浏览器使用开发者 DeepSeek Key 完成一次真实技能提取，模型成功返回 7 项技能，但热更新缓存的旧仓储实例不接受新增 `model` 参数，导致用量回写阶段出现 `TypeError`。
- 用量服务增加严格限定的旧接口兼容分支：只在 `model` 关键字不受支持时回退一次，继续写入调用状态、Token 和费用，不重复请求模型。
- 仓储缓存修订号递增，部署更新后主动创建新版仓储实例；兼容分支只处理热更新短暂窗口。
- 本次故障调用已在管理员 Dashboard 记为 1 次，但因旧接口异常发生在回写前，该条历史记录无法补录真实 Token；后续调用不受影响。

### 回归验证

- 新增旧仓储缓存兼容测试，验证模型结果只处理一次且 Token 仍能落库。
- 120 项自动化测试、Ruff、格式和差异检查将在提交前统一验证。

## 2026-09-24 C08-D0 本地开发

- 审计七张 SQLite 业务表并新增 `schema_migrations`；生产通过标准 PostgreSQL URL 与 TLS，缺失或不可用时安全停止。
- 加入合成 SQLite→PostgreSQL 迁移、独立数据库 CI job、受控生产验证记录与长期加密主密钥部署说明。
- Kimi 与 MiniMax 精确 ID 已按官方目录复核；GLM 官方页面请求超时，保持未验证候选。大陆真实模型验证、远端 CI 和备份恢复仍待证据，不推送或部署。
